name: flutter_inject0r
description: "A lightweight dependency injection library for Flutter & Jaspr"
version: 0.0.1
homepage: https://www.github.com/frederikstonge/inject0r
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ^3.41.0

dependencies:
  flutter:
    sdk: flutter
  inject0r: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter