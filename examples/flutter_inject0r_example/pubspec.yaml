name: flutter_inject0r_example
description: "A new Flutter project."
resolution: workspace

version: 1.0.0+1

environment:
  sdk: ^3.11.0
  flutter: ^3.41.0

dependencies:
  bloc: ^9.0.0
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  go_router: ^17.2.3
  inject0r: ^0.0.1
  inject0r_example: ^1.0.0
  flutter_inject0r: ^0.0.1
  flutter_inject0r_bloc: ^0.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true